print("Area of a trapezoid")

height = input("Enter the height of the trapezoid: ")
bottom_length = input("Enter the length of the bottom base: ")
top_length = input("Enter the length of the top base: ")
output = (height / 2) * (bottom_length + top_length)

print("The area is: " + str(output))